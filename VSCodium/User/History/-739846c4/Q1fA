#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '

#The following lines set the powerline up redundant with starship

#powerline-daemon -q
#POWERLINE_BASH_CONTINUATION=1
#POWERLINE_BASH_SELECT=1
#. /usr/share/powerline/bindings/bash/powerline.sh

#The following line runs neofetch in every opened terminal
neofetch

# The following will apply the color scheme set by pywall to all future opened terminals

# Import colorscheme from 'wal' asynchronously
# &   # Run the process in the background.
# ( ) # Hide shell job control messages.
(cat ~/.cache/wal/sequences &)

# To add support for TTYs this line can be optionally added.
source ~/.cache/wal/colors-tty.sh

# The following line starts starship
eval "$(starship init bash)"

# Global variable for wallpaper location
export WALLPAPERS=~/./Downloads/wallpapers

# Global variable for starship config
#export STARSHIP_CONFIG=~/.config/starship/starship.toml

#Add conda to path variables, to be able to use it in shell
export PATH="$PATH:~/miniconda3/bin"

# The following should only be uncommented when in use, slows down shell launch significantly.
# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!


__conda_setup="$('/home/squarewinter/miniconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/home/squarewinter/miniconda3/etc/profile.d/conda.sh" ]; then
        . "/home/squarewinter/miniconda3/etc/profile.d/conda.sh"
    else
        export PATH="/home/squarewinter/miniconda3/bin:$PATH"
    fi
fi
unset __conda_setup


# <<< conda initialize <<<
# !!IMPORTANT, remember always use conda install first, pip only works after

